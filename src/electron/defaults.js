'use strict'

const DEFAULT_SETTINGS = {
  shortcut: 'Command+Alt+X',
  fontface: 'Courier New',
  theme: 'prism-tomorrow',
  subset: 'js, css, html',
  autopaste: true
}

module.exports = {
  DEFAULT_SETTINGS
}
