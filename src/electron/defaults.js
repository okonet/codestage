'use strict'

const DEFAULT_SETTINGS = {
  shortcut: 'CommandOrControl+Alt+X',
  fontface: 'Courier New',
  theme: 'xcode',
  subset: 'js, css, html',
  autopaste: true
}

module.exports = {
  DEFAULT_SETTINGS
}
