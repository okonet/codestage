'use strict'

const DEFAULT_SETTINGS = {
  shortcut: 'Command+Alt+V',
  fontface: 'Courier New',
  fontsize: 64, // 32pt
  theme: 'default',
  subset: '',
  lastUsedLanguage: null,
  autopaste: true
}

module.exports = {
  DEFAULT_SETTINGS
}
