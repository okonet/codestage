'use strict'

const DEFAULT_SETTINGS = {
  shortcut: 'Command+Alt+X',
  fontface: 'Courier New',
  theme: 'github',
  subset: 'jsx, css, html',
  lastUsedLanguage: null,
  autopaste: true
}

module.exports = {
  DEFAULT_SETTINGS
}
