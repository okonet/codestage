'use strict'

const DEFAULT_SETTINGS = {
  shortcut: 'Command+Alt+X',
  fontface: 'Courier New',
  theme: 'github',
  subset: 'jsx, css, html',
  autopaste: true
}

module.exports = {
  DEFAULT_SETTINGS
}
